- defaults:
    link:
      relink: true

- clean: ["~", "~/AppData/Local"]

- link:
    ~/.config/nvim: nvim
    ~/.wezterm.lua: .wezterm.lua
    ~/.config/starship.toml:
      path: starship/starship.toml
      create: true

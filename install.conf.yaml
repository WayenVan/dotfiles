- defaults:
    link:
      relink: true

- clean: ["~", "~/AppData/Local"]

- create:
    - ~/.config

- link:
    ~/.config/nvim: nvim
    ~/.wezterm.lua: .wezterm.lua
    ~/.config/starship.toml: starship/starship.toml
    ~/.condarc:
      path: conda/.condarc

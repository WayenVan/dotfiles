- defaults:
    link:
      relink: true

- clean: ["~", "~/.config"]

- create:
    - ~/.config

- link:
    ~/.config/nvim: nvim
    ~/.config/starship.toml: starship/starship.toml
    ~/.condarc: conda/.condarc
    ~/.config/wezterm: wezterm
    ~/.config/fish/: fish
